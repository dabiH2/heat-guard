# Copy to .env and paste your key. .env is gitignored — never commit it.
# Key arrives by email from support@fortyguard.com ("Your FortyGuard API key is ready").
FORTYGUARD_API_KEY=paste_key_here

# Optional: model provider for agent.py narration
ANTHROPIC_API_KEY=
